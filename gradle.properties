#==================================
# Core Build Configuration
#==================================
# Disable daemon for Docker builds
org.gradle.daemon=false

# Build performance settings
org.gradle.parallel=true
org.gradle.caching=true
#org.gradle.configureondemand=true

# JVM arguments optimized for containers
# org.gradle.jvmargs=-Xmx1g -XX:MaxMetaspaceSize=256m -XX:+UseContainerSupport

#==================================
# Build Behavior
#==================================
# Logging and debugging
org.gradle.warning.mode=all
org.gradle.logging.stacktrace=full
org.gradle.console=plain

#==================================
# Dependency Management
#==================================
# Enable dependency verification
#org.gradle.dependency.verification.console=verbose

#==================================
# Build Output
#==================================
# Ensure consistent output
#org.gradle.internal.publish.checksums.insecure=false